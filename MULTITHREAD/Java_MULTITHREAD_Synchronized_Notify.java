package MULTITHREAD;

public class Java_MULTITHREAD_Synchronized_Notify extends Java_MULTITHREAD_Synchronized_Wait {
    public static String notifyMethod = "\"final void notify()\" (notify()) method:" +
            "\n\t- wakes up a thread that called wait( ) on the same object.";
}
