package MULTITHREAD;

public class Java_MULTITHREAD_Synchronized_Wait {
    public static String waitMethod = "\"final void wait() throws InterruptedException\" (wait()) method:" +
            "\n\t- tells the calling thread to give up the monitor and go to sleep until some other\n" +
            "thread enters the same monitor and calls notify( ) or notifyAll( ).";
}
